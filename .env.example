
# ── Frontend (safe to be public; shipped in the browser bundle) ──
VITE_SUPABASE_URL=https://YOURPROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...your-anon-public-key...

# ── Scraper only (SECRET — never prefix these with VITE_, never commit) ──
SUPABASE_URL=https://YOURPROJECT.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGci...your-service_role-key...
# Tauri auth: the persistent credential cookies from a logged-in browser
# (devtools → Application → Cookies → tauriwow.com). `pasw` is password-equivalent —
# treat it like a password. Static: only changes if you change your Tauri password.
TAURI_COOKIE=username=YOURNAME; pasw=PASTE_YOUR_PASW_COOKIE_HASH_HERE
DEFAULT_REALM=[EN] Evermoon
